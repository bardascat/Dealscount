var url = "http://localhost/dealscount/";$(document).ready(function() {   })function ajax_login() {    var form = $('#header_login_popup form').serialize();    $.ajax({        type: "POST",        url: url + "user/ajax_login_submit",        data: form,        dataType: 'json',        success: function(result) {            if (result.type == "success") {                if (result.action == "create_user") {                                      var img = document.createElement("img");                    var trackUrl = "http://www.googleadservices.com/pagead/conversion/997185351/?value=0&amp;label=3fPMCPHqkAcQx66_2wM&amp;guid=ON&amp;script=0";                    img.src = trackUrl;                    document.body.appendChild(img);                }                $('#header_login_popup .msg').html('');                location.reload();            }            else {                $('#header_login_popup .msg').html(result.msg);            }            console.log(result);        }})    return false;}